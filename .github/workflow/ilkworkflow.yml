name: Ilk Workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  Workflow_dispatch:

jobs:
  deneme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Hello World
        run: 
          echo "Hello, World!"
          printenv